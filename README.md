# miniMoE

Small PyTorch mixture-of-experts transformer experiment.

The project includes a simple MoE model, a tokenizer/data helper, and a Shakespeare text dataset for local experimentation.

Requires PyTorch and tiktoken.
